name: persalone_horizon_companion
description: HORIZON Studio Companion — local host that owns app runs on the official emulator and serves the Studio API on loopback.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.6.0
resolution: workspace
dependencies:
  crypto: ^3.0.3
  persalone_contracts:
    path: ../../packages/contracts
  persalone_halo_core:
    path: ../../packages/halo_core
  persalone_halo_emulator:
    path: ../../packages/halo_emulator
dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
