name: persalone_mobile
description: Minimal Android and iOS shell for the PersalOne Halo runtime.
version: 0.1.0+1
publish_to: none
environment:
  sdk: ^3.6.0
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  persalone_contracts:
    path: ../../packages/contracts
  persalone_audio_adapters:
    path: ../../packages/audio_adapters
  persalone_translation_runtime:
    path: ../../packages/translation_runtime
dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.1
flutter:
  uses-material-design: true
