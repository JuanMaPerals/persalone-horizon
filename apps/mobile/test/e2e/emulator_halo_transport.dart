// The emulator transport moved to packages/halo_emulator so the Studio
// Companion can use it; the E2E suites keep importing it from here.
export 'package:persalone_halo_emulator/persalone_halo_emulator.dart';
